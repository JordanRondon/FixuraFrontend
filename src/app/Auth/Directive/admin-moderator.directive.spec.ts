import { AdminModeratorDirective } from './admin-moderator.directive';

describe('AdminModeratorDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminModeratorDirective();
    expect(directive).toBeTruthy();
  });
});
