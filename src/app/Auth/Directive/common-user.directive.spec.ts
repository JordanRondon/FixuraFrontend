import { CommonUserDirective } from './common-user.directive';

describe('CommonUserDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonUserDirective();
    expect(directive).toBeTruthy();
  });
});
